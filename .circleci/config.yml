version: 2.1
commands:
 mydemocmd:
  description: demo for commands reuse
  steps:
   - run: echo ${CIRCLE_WORKFLOW_ID}

jobs:
  my_job:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - mydemocmd
      - run: echo "Ok"
      - run:
           name: ALERTING 
           command: echo "ERROR HAPPENED"
           when: on_fail


   



workflows:
  my_workflow:
    jobs:
      - my_job
      
    
 

