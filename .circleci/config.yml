version: 2.1
command:
 mydemocmd:
  description: demo for commands reuse
  steps:
   - run:  ${CIRCLE_WORKFLOW_ID}

jobs:
  my_job:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - mydemocmd

workflows:
  my_workflow:
    jobs:
      - my_job
    
 

